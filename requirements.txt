asgiref==3.8.1
Django==5.2
djangorestframework==3.16.0
sqlparse==0.5.3
tzdata==2025.2
celery==5.3.5
django-celery-beat>=2.7.0
flower==2.0.1
django-cors-headers==4.3.1
redis==5.2.1
djangorestframework-simplejwt==5.3.0
python-decouple==3.8
pandas
matplotlib
scikit-learn
scipy
seaborn
openpyxl
gunicorn==21.2.0
pycaret

# Optimizaciones GPU NVIDIA (opcionales - requieren CUDA)
# cuml>=0.15  # RAPIDS cuML para aceleración GPU (requiere Python <3.11)
# cudf>=0.15  # RAPIDS cuDF para DataFrames en GPU (requiere Python <3.11)
# cupy-cuda11x  # CuPy para operaciones numéricas en GPU
torch>=2.0.0  # PyTorch con soporte CUDA/ROCm
pynvml>=11.0.0  # NVIDIA Management Library

# Optimizaciones GPU AMD (opcionales - requieren ROCm)
# torch-rocm  # PyTorch compilado con soporte ROCm para AMD
# tensorflow-rocm  # TensorFlow compilado con soporte ROCm para AMD

# Optimizaciones CPU
psutil>=5.8.0  # Para monitoreo de recursos del sistema
numba>=0.56.0  # Compilación JIT para acelerar operaciones numéricas
scikit-learn-intelex  # Optimizaciones Intel para scikit-learn
py-cpuinfo>=9.0.0  # Para información detallada del CPU
nvidia-ml-py3>=7.352.0  # Para detección de GPU NVIDIA (opcional)

# Versiones específicas para mejor rendimiento
xgboost>=1.6.0  # Soporte mejorado para GPU
lightgbm>=3.3.0  # Soporte para GPU
catboost>=1.0.0  # Soporte nativo para GPU